import 'package:flutter/material.dart';

class ShowsView extends StatelessWidget {
  const ShowsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("TV shows"));
  }
}
